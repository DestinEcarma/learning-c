#include <stdio.h>
#include <stdlib.h>

typedef struct node {
	int data;
	struct node *next;
} Node;

Node *createNode(int data) {
	Node node = malloc(sizeof(Node));
	
	
}

int main(void) {
	int size;
	int digit;

	for (int i = 0; i < size; i++) {
		scanf("%d", &digit);
		
	}
	
	return 0;
}